<?php

declare(strict_types=1);

return [
    'enabled' => env('ADMIN_PULSE_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | Sample rates per kind (0..1)
    |--------------------------------------------------------------------------
    | rate=1.0 — пишем всё; 0.1 — 10% выборка; 0.0 — отключено.
    */

    'sample_rate' => [
        'request' => 0.1,
        'query' => 0.1,
        'job' => 1.0,
        'exception' => 1.0,
        'cache' => 0.05,
    ],

    /*
    |--------------------------------------------------------------------------
    | Routes для пропуска
    |--------------------------------------------------------------------------
    | request->is($pattern) — paths, которые middleware не сэмплирует.
    | По умолчанию — pulse-сам-себя + health endpoint'ы.
    */

    'ignore_routes' => [
        'api/admin/system-pulse-samples/*',
        '_debugbar/*',
    ],

    /*
    |--------------------------------------------------------------------------
    | TTL для cleanup
    |--------------------------------------------------------------------------
    */

    'retention' => [
        'samples_hours' => 24,
        'aggregates_days' => 7,
    ],

    /*
    |--------------------------------------------------------------------------
    | SQL fingerprint
    |--------------------------------------------------------------------------
    */

    'fingerprint' => [
        'sql_strip_values' => true,
    ],
];
